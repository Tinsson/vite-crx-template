# @vite-crx/shared

Shared protocol types, constants, and parsers for the Vite CRX monorepo. Exports fixed server configuration, HTTP/WebSocket message types, and runtime validation — no platform dependencies. Consumed as raw TypeScript source by Vite (extension) and Bun (server).
